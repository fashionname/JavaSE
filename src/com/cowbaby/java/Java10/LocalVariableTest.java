package com.cowbaby.java.Java10;

public class LocalVariableTest {

    /*
        局部变量推断

        var num = 10;

        // 不赋值不能推断
        // lambda 表达式不能 var lambda = (a, b) -> a + b; 不能
        // 方法引用 var con = System.out::println 不能
        // 为数组静态初始化 var arr = {1, 2, 3, 4}

        var 不是关键字
     */
}
